# Decon AI — pinned to the exact versions the app runs on locally, so the
# hosted environment is a byte-for-byte clone (guarantees identical behaviour).
# Regenerate after adding a dependency:  pip freeze > requirements.txt (then trim).

# ── core app ──
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0   # pinned — an unpinned pyarrow re-resolving on redeploy segfaults on Streamlit Cloud
requests==2.34.2
python-dotenv==1.2.2

# ── scraping / parsing (HTTP fetcher — no browser needed) ──
scrapling==0.4.10
beautifulsoup4==4.15.0
lxml==6.1.1

# ── dashboards & reports ──
plotly==6.8.0
openpyxl==3.1.5
pillow==12.2.0

# ── deep review sampling (Product Intelligence / Review Intelligence) ──
apify-client==3.0.3
